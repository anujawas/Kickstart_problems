#include <bits/stdc++.h>

using namespace std;
#define ll long long int 
#define boost  ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

int main(){
    boost;
    ll t;
    cin>>t;
    for(ll j=0;j<t;j++){
    	ll d,n,k;
    	ll* arr=new ll[k];
    	cin>>d>>n>>k;

    }
    return 0;
}